<odoo>
    <record id="view_purchase_order_lines_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- Agregar el campo analytic_distribution después del campo name en las líneas de orden de compra -->
            <xpath expr="//form/sheet/notebook/page[@name='products']/field[@name='order_line']/tree/field[@name='name']" position="after">
                <field name="analytic_distribution" widget="analytic_distribution" />
            </xpath>
        </field>
    </record>
</odoo>
